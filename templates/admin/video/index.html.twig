{% extends 'admin/layout/base.html.twig' %}
{% block body %}
    <h1>Liste des Video</h1>

    <table id="table_id" class="table table-striped">
        <thead>
            <tr>
                <th>Date Création</th>
                <th>Titre</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for blog in blogs %}
            <tr>
                <td>{{ blog.datecreation ? blog.datecreation|date('Y-m-d') : '' }}</td>
                <td>{{ blog.titre }}</td>
                <td><img src="{{asset('images/blogs/'~blog.imageVideo)}}" height="40"></td>
                <td>
                    <a class="btn btn-primary btn-sm" href="{{ path('admin_video_show', {'id': blog.id}) }}">Détails</a>
                    <a class="btn btn-warning btn-sm" href="{{ path('admin_video_edit', {'id': blog.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_video_new') }}">Ajouter un video?</a>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/datatables.min.css') }}" rel="stylesheet" type="text/css">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <!-- DataTable-->
    <script src="{{ asset('assets/js/pdfmake.min.js') }}"></script>
    <script src="{{ asset('assets/js/vfs_fonts.js') }}"></script>
    <script src="{{ asset('assets/js/datatables.min.js') }}"></script>

    <script>
        $(document).ready(function () {
            $('#table_id').DataTable({
                language: {
                    url: 'http://cdn.datatables.net/plug-ins/1.10.24/i18n/French.json'
                },
            });
        });
    </script>
{% endblock %}
