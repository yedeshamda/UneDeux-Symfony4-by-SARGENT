{% extends 'layout/base.html.twig' %}
{% block body %}
    <h1>Détails du Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ marque.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ marque.image }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ marque.slug }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('marque/_delete_form.html.twig') }}

    <br>
    <a href="{{ path('admin_marque_edit', {'id': marque.id}) }}">Modifer</a>
    <br>
    <a href="{{ path('admin_marque_index') }}">back to list</a>
    <br>
{% endblock %}
