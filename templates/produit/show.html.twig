{% extends 'layout/base.html.twig' %}
{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ produit.datecreation ? produit.datecreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ produit.slug }}</td>
            </tr>
            <tr>
                <th>Descriptiontech</th>
                <td>{{ produit.descriptiontech }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_produit_index') }}">back to list</a>

    <a href="{{ path('admin_produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('produit/_delete_form.html.twig') }}

    <br>
    <a href="{{ path('admin_user_edit', {'id': user.id}) }}">Modifer</a>
    <br>
    <a href="{{ path('admin_user_index') }}">back to list</a>
    <br>
{% endblock %}
